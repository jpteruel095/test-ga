# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Arventa Connect' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Arventa Connect
  pod 'Alamofire' # API Communicator
  pod 'SwiftyJSON' # Handling JSON Data from API
  pod 'SwiftDate'
  pod 'ObjectMapper'
  pod 'ReachabilitySwift'
  
  #UI
  pod 'IBAnimatable'
  pod 'IQKeyboardManagerSwift'
  pod 'MBProgressHUD'
  pod 'SideMenu'
  pod 'JVFloatLabeledTextField'
  
  # Firebase
  pod 'Firebase/Analytics'
  pod 'Firebase/Crashlytics'
  
  target 'Arventa ConnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Arventa ConnectUITests' do
    # Pods for testing
  end

end
